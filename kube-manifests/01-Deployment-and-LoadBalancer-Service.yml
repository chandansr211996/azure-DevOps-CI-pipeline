apiVersion: apps/v1
kind: Deployment
metadata:
  name: chandansrazuredevopscipipeline-nginx-deployment
  labels:
    app: chandansrazuredevopscipipeline-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chandansrazuredevopscipipeline-nginx
  progressDeadlineSeconds: 600  # Increase progress deadline
  template:
    metadata:
      labels:
        app: chandansrazuredevopscipipeline-nginx
    spec:
      containers:
        - name: chandansrazuredevopscipipeline-nginx
          image: askcontainerDT.azurecr.io/chandansrazuredevopscipipeline:46
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: chandansrazuredevopscipipeline-nginx-loadbalancer-service
  labels:
    app: chandansrazuredevopscipipeline-nginx
spec:
  type: LoadBalancer
  selector:
    app: chandansrazuredevopscipipeline-nginx
  ports:
    - port: 80
      targetPort: 80
